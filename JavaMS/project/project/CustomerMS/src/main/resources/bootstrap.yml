server:
  port: 8070
  
spring:
  application:
    name: CustomerMS
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefixes: config2
        defaultContext: application
        profileSeparator: '::'
        format: YAML  