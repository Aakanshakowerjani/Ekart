server:
  port: 8071
  
spring:
  application:
    name: CustomerCartMS
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefixes: config2
        defaultContext: application
        profileSeparator: '::'
        format: YAML  